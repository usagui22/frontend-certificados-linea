import React, { useState } from "react";
import { Form } from "react-bootstrap";

export const FormActualizarUnidad =()=>{
    const[dato, setDatos]=useState(false);
    return(
        <>
        <Form>
            
        </Form>
        </>
    );
}